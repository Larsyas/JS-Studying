/**
 * FALSY
 * 0
 * '' "" `` (empty strings)
 * null / undefined
 * NaN
 */
// console.log('clovis' && 'Maria');
// console.log(0 || false || null || 'Clovis' || true);
const corUsuario = 'bleu';
const corPadrao = corUsuario || 'red';

console.log(corPadrao);
